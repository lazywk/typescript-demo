export default function Home() {
  // const signedIn = useSelector((state: RootState) => state.auth)

  return (
    <main className="container mx-auto px-2 bg-[#262626]">
      <div className="flex items-center justify-center h-screen items-center flex-col">
        <p>Home Page</p>
      </div>
    </main>
  )
}
